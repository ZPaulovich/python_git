""" 2.2 Торговая фирма в первый день работы реализовала товаров на P тыс. руб.,
# а затем ежедневно увеличивала выручку на 3%. (вот только не укезано 3% от первоначального Р или текущего...)
# Какой будет выручка фирмы в тот день, когда она впервые превысит заданное значение Q?
# Сколько дней придется торговать фирме для достижения этого результата?
"""
n = 0
p = float(input("Введите размер выручки:\n"))
q = float(input("Введите размер ожидаемой суммарной выручки:\n"))
s = p
# расчет, если увеличение в 3% постоянно только от первоначаального Р
while s <= q:
    s += p*3./100       # суммирование выручки
    n += 1               # подсчет дней
print("Сумма1, превышающая ожидаемую суммарную выручку = " + str(s))
print("Затраченное количество дней = " + str(n))
# расчет, если увеличение в 3% от текущего Р
n = 0
while p <= q:
    p += p*3./100       # суммирование нарастающей выручки
    n += 1               # подсчет дней
print("Сумма, превышающая ожидаемую суммарную выручку = " + str(p))
print("Затраченное количество дней = " + str(n))
